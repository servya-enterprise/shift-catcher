# Bootstrap Prompt for Codex

Use este texto ao criar o projeto novo:

> Leia `AGENTS.md` e `00-Start/00-Home.md`. Este é um projeto separado do Clara Care, mas deve reutilizar as convenções de engenharia especificadas em `02-Architecture/Clara-Care-Reuse-Strategy.md`. Valide todos os documentos, o draft OpenAPI, o DAG de work packages e a endpoint coverage. Não implemente funcionalidades fora do WP READY. Comece por `WP-POC-001`. Lacunas reversíveis devem virar AUTODEC; não altere DEC ou POC Freeze. O objetivo é validar cedo o transporte real GREEN-API em grupo comum, antes de construir o pipeline completo.

Depois de WP-001, fornecer credenciais reais somente via secrets locais para executar o gate manual de `WP-POC-002`.
